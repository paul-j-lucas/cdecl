EXPECTED_EXIT=0
cdecl -bEL$LINENO --config=data/config_tdu.cdeclrc <<END
explain c_ast_id_t x
END

cdecl -bEL$LINENO --config=data/config_using_i.cdeclrc <<END
explain Int x
END

export CDECLRC=/does/not/exist
echo | cdecl -b

export CDECLRC=/tmp/is_not_readable
touch $CDECLRC
chmod 000 $CDECLRC
echo | cdecl -b
rm -f $CDECLRC
unset CDECLRC

# vim:set syntax=sh et sw=2 ts=2:
