EXPECTED_EXIT=66
echo | cdecl -bE --config=/tmp/does_not_exist

EXPECTED_EXIT=65
# The EOF from the pipe will cause cdecl to exit rather than be interactive in
# the event it doesn't exit from the error.
echo | cdecl -bE --config=data/error.cdeclrc

# vim:set syntax=sh et sw=2 ts=2:
